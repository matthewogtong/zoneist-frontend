import React from 'react'
// import { useSelector } from 'react-redux'

const CompletedZone = () => {
    // const currentZone = useSelector(state => state.user.entities[0].zones.slice(-1)[0])

    return (
        <p>you have completed the zone!</p>
    )
}

export default CompletedZone